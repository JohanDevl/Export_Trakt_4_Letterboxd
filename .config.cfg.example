# Trakt API credentials - Get these from https://trakt.tv/oauth/applications
API_KEY="YOUR_API_KEY_HERE"
API_SECRET="YOUR_API_SECRET_HERE"
ACCESS_TOKEN=""
REFRESH_TOKEN=""
REDIRECT_URI="urn:ietf:wg:oauth:2.0:oob"

# Trakt user information
USERNAME="YOUR_TRAKT_USERNAME"
API_URL="https://api.trakt.tv"

# Directory paths - You can customize these
BACKUP_DIR="./backup/$(date +"%Y-%m-%d_%H-%M-%S")_trakt-backup"
DATE=$(date +%Y%m%d_%H%M)
DOSLOG=./logs
LOG=${DOSLOG}/${DATE}-Export_Trakt.txt
DOSCOPY=./copy
BRAIN_OPS=./brain_ops

# Terminal colors - No need to modify these
RED='\033[0;31m'
GREEN='\033[0;32m'
NC='\033[0m' # No Color 
BOLD='\033[1m'
SAISPAS='\e[1;33;41m'

# Directory creation - No need to modify these
if [ -d ./TEMP ]
	then
	rm -r ./TEMP
fi
mkdir TEMP
if [ ! -d $DOSLOG ]
	then
	mkdir $DOSLOG 